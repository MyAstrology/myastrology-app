// Client-side allowlist gating access to the in-app Admin Panel (SettingsScreen
// only shows the entry, AdminScreen re-checks this too). Add the Google
// account email(s) that should see admin data here.
export const ADMIN_EMAILS = [
  // 'owner@example.com',
];
