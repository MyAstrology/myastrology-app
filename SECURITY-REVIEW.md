# নিরাপত্তা পর্যালোচনা — MyAstrology অ্যাপ

**তারিখ:** ২ অগাস্ট ২০২৬ · **সংস্করণ:** ১.০.২ (বিল্ড ৪)
**পরিধি:** অ্যাপের কোড, Firebase নিয়ম, Cloud Functions, WebView সেটিংস,
ব্যক্তিগত তথ্যের ব্যবহার।

> **এক বাক্যে:** কোনো গুরুতর ফাঁক পাওয়া যায়নি। মূল কাঠামো — কে কার তথ্য
> দেখতে পাবে — ঠিকভাবে বানানো। তিনটে জায়গা শক্ত করা হয়েছে, আর দুটো কাজ
> আপনার করার আছে (কোডের বাইরে)।

---

## ✅ যা ঠিক আছে (কিছু করার নেই)

**১. কে কার তথ্য দেখতে পায় — সঠিক**
`firestore.rules`-এ প্রত্যেক ব্যবহারকারী **কেবল নিজের** ডকুমেন্ট ও নিজের
সংরক্ষিত কুণ্ডলী পড়তে/লিখতে পারেন। অন্য কারো তথ্য পড়ার কোনো পথ নেই।

**২. অ্যাডমিন-সুরক্ষা সার্ভারে, ক্লায়েন্টে নয়**
ব্যবহারকারী ব্লক/ডিলিট করার ক্ষমতা Cloud Function-এ, যেখানে Google-এর
যাচাইকৃত লগইন-টোকেন থেকে ইমেইল দেখে অনুমতি দেওয়া হয়। অ্যাপের কোড বদলে
কেউ অ্যাডমিন সাজতে পারবে না। নিজেকে ব্লক বা ডিলিট করাও আটকানো আছে।

**৩. লগইন ব্রিজে ক্ষমতা বাড়ানোর সুযোগ নেই**
`mintWebViewToken` কেবল **যে ব্যবহারকারী নিজে কল করছেন তাঁরই** টোকেন
বানায় — অন্য কারো uid দিয়ে টোকেন চাওয়ার কোনো উপায় নেই।

**৪. লগইন পদ্ধতি নিরাপদ**
কেবল Google Sign-In। কোনো পাসওয়ার্ড অ্যাপে রাখা বা পাঠানো হয় না।

**৫. পরিসংখ্যানে ব্যক্তিগত তথ্য যায় না**
Firebase Analytics-এ কেবল **স্ক্রিনের নাম** পাঠানো হয় (যেমন "Kundali")।
কারো নাম, জন্মতারিখ বা কুণ্ডলী কখনো পাঠানো হয় না। বিজ্ঞাপন-শনাক্তকারী
(AAID) সংগ্রহও `plugins/withDisableAdId.js` দিয়ে বন্ধ করা।

**৬. ফোনে জমা তথ্য**
স্থানীয় প্রোফাইল (রাশি, পছন্দ, পঞ্চাঙ্গের শহর) ফোনের ভিতরেই থাকে,
অ্যান্ড্রয়েডের অ্যাপ-স্যান্ডবক্সে — অন্য অ্যাপ পড়তে পারে না।

---

## 🔒 যা এই পর্যালোচনায় শক্ত করা হয়েছে

**১. HTTP-র মাধ্যমে আক্রমণের পথ বন্ধ** *(কোডে সম্পন্ন)*

আগে তিনটে WebView-ই `mixedContentMode="always"` ও অনুমোদিত তালিকায়
`http://*` রাখত। এর মানে — সুরক্ষিত (https) পাতার ভিতরেও অরক্ষিত (http)
বিষয়বস্তু লোড হতে পারত। **ঝুঁকি:** পাবলিক/হোটেল/ক্যাফের ওয়াই-ফাইতে কেউ
মাঝপথে ওই অরক্ষিত অংশটা বদলে দিয়ে পাতায় নিজের কোড ঢোকাতে পারত।

যাচাই করে দেখা গেছে বান্ডল করা **কোনো পাতাই http:// ব্যবহার করে না**,
তাই বন্ধ করাতে কিছুই ভাঙে না।

**২. অ্যাডমিন যাচাইয়ে যাচাইকৃত-ইমেইল শর্ত** *(কোডে সম্পন্ন — deploy লাগবে)*

আগে শুধু ইমেইল ঠিকানা মিলিয়ে দেখা হতো। আজ ঝুঁকি নেই (Google Sign-In-এ
ঠিকানা সবসময় যাচাইকৃত), কিন্তু ভবিষ্যতে ইমেইল/পাসওয়ার্ড লগইন যোগ করলে
কেউ অ্যাডমিনের ঠিকানা দিয়ে অ্যাকাউন্ট খুলে সরাসরি অ্যাডমিন হয়ে যেতে
পারত। এখন `email_verified` না থাকলে অনুমতি মেলে না।

⚠️ এটা Cloud Function-এ, তাই **deploy না করা পর্যন্ত কার্যকর হবে না**:
```
firebase deploy --only functions
```

---

## 📋 আপনার করার কাজ (কোডের বাইরে)

**১. Firebase API কী সীমাবদ্ধ করুন** — *গুরুত্ব: মাঝারি*

`google-services.json`-এ যে কী আছে, সেটা **ফাঁস নয়** — Firebase-এর
অ্যান্ড্রয়েড/ওয়েব কী প্রকাশ্য হওয়ারই কথা, সেটাই স্বাভাবিক নকশা। আসল
সুরক্ষা আসে Firestore নিয়ম থেকে, যা ঠিক আছে।

তবু ভালো অভ্যাস: Google Cloud Console → APIs & Services → Credentials →
ওই কী-তে **Application restrictions** দিন (Android অ্যাপ: প্যাকেজ
`in.myastrology.app` + আপনার SHA-1)। এতে অন্য কেউ আপনার কী দিয়ে নিজের
অ্যাপ চালিয়ে আপনার কোটা খরচ করতে পারবে না।

**২. Firebase App Check** — *গুরুত্ব: মাঝারি · ⚠️ যত সহজ ভেবেছিলাম তত নয়*

App Check নিশ্চিত করে যে Firestore/Functions-এ অনুরোধ **সত্যিই আপনার অ্যাপ
থেকেই** আসছে, কোনো স্ক্রিপ্ট থেকে নয়।

**নিবন্ধন (Register) করা সম্পূর্ণ নিরাপদ** — Firebase Console → App Check →
Apps → MyAstrology Android → Play Integrity। এতে কিছুই বন্ধ হয় না।

**🚫 কিন্তু "APIs" ট্যাবে গিয়ে Enforce চালু করবেন না** — কারণ:

এই অ্যাপে দুই রকম Firebase SDK একসাথে চলে:
  · Auth · Firestore · Functions → **JS SDK** (`firebase` npm প্যাকেজ)
  · Analytics                    → **নেটিভ SDK** (`@react-native-firebase`)

Play Integrity দিয়ে App Check কাজ করে **নেটিভ** SDK-তে। কিন্তু অ্যাপের
আসল ডেটা-কল (লগইন, প্রোফাইল, অ্যাডমিন) সব **JS** SDK দিয়ে হয়, আর নেটিভ
দিকের App Check টোকেন JS SDK-র অনুরোধে আপনাআপনি যুক্ত হয় না — দুটো আলাদা
ব্যবস্থা।

অর্থাৎ শুধু প্যাকেজ যোগ করলেই হবে না। Enforce চালু করলে অ্যাপের সব
ডেটা-কল আটকে যাবে — লগইন, সংরক্ষিত কুণ্ডলী, অ্যাডমিন প্যানেল সব বন্ধ।

**সঠিক পথ (পরে, আলাদা কাজ হিসেবে):** নেটিভ দিক থেকে App Check টোকেন নিয়ে
JS SDK-র `CustomProvider`-এ জুড়ে দেওয়া। এটা করা যায়, কিন্তু ডিভাইসে
পরীক্ষা না করে ছাড়া নিরাপদ নয় — ভুল হলে সব ব্যবহারকারীর অ্যাপ অচল হবে।
তাই বর্তমান রিলিজ ফোনে যাচাই হওয়ার পর, আলাদা করে করা উচিত।

---

## ⏸️ একটা প্রস্তাব — আপনার সম্মতি লাগবে

**`orders` কালেকশনে অপব্যবহারের সুযোগ**

ওয়েবসাইটের চেকআউটে গ্রাহক **লগইন ছাড়াই** অর্ডার তৈরি করেন — সেটা
ব্যবসার প্রয়োজনেই ঠিক আছে, আর অর্ডার **পড়তে** পারেন কেবল অ্যাডমিন
(ব্যক্তিগত তথ্য সুরক্ষিত ✓)।

কিন্তু **তৈরি** করায় কোনো সীমা নেই। কেউ ইচ্ছে করলে স্ক্রিপ্ট চালিয়ে
লক্ষ লক্ষ ভুয়া অর্ডার বানিয়ে দিতে পারে — এতে তথ্য ফাঁস হবে না, কিন্তু
**আপনার Firebase বিল বাড়বে** এবং অ্যাডমিন প্যানেল আবর্জনায় ভরে যাবে।

প্রস্তাবিত সংশোধন (`firestore.rules`-এ `orders`-এর `allow create`):

```
allow create: if request.resource.data.keys().hasAll(['pid','email','ts'])
              && request.resource.data.keys().size() < 30
              && request.resource.data.email is string
              && request.resource.data.email.size() < 200
              && request.resource.data.pid is string
              && request.resource.data.pid.size() < 100
              && request.resource.data.ts is number
              && request.resource.data.ts > request.time.toMillis() - 300000
              && request.resource.data.ts < request.time.toMillis() + 300000;
```

অর্থাৎ: ফিল্ডের সংখ্যা ও লেখার দৈর্ঘ্যে ছাদ, আর সময়টা এখনকার ৫ মিনিটের
মধ্যে হতে হবে।

**কেন নিজে থেকে করিনি:** এটা **লাইভ ওয়েবসাইটের আসল চেকআউট**-এ প্রভাব
ফেলে। ভুল হলে সত্যিকারের গ্রাহকের অর্ডার আটকে যেতে পারে — অর্থাৎ সরাসরি
টাকার ক্ষতি। তাই আগে আপনার সম্মতি, তারপর ওয়েবসাইটের চেকআউট আসলে কোন
কোন ফিল্ড পাঠায় তা মিলিয়ে দেখে, তারপর প্রয়োগ।

---

## 🔍 যা ঝুঁকি হিসেবে দেখা হয়েছে কিন্তু গ্রহণযোগ্য

**`allowUniversalAccessFromFileURLs` চালু**
বান্ডল করা পাতাগুলো যেন ওয়েবসাইটের সাথে কথা বলতে পারে (লগইন সিঙ্ক, ছবি,
লাইভ তথ্য) — তার জন্য এটা দরকার। ঝুঁকি তখনই হতো যদি বাইরের কারো লেখা
বিষয়বস্তু ওই পাতায় দেখানো হতো; এখানে সব পাতাই আপনার নিজের, আর
ব্যবহারকারীর দেওয়া তথ্য কেবল তাঁর নিজের ফোনেই থাকে।
**তবে ভবিষ্যতে** যদি কখনো ব্যবহারকারীর মন্তব্য/রিভিউ ওই পাতায় দেখানো
হয়, তখন এটা আবার বিবেচনা করতে হবে।

---

## পরবর্তী পর্যালোচনা কখন

- Play Billing যোগ করার পর (টাকা-সংক্রান্ত নতুন কোড আসবে)
- কোনো নতুন লগইন পদ্ধতি যোগ করলে
- ব্যবহারকারীর লেখা কোনো বিষয়বস্তু (রিভিউ/মন্তব্য) দেখানো শুরু করলে
